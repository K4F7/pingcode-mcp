{
  "name": "pingcode-mcp",
  "version": "0.1.0",
  "description": "MCP server for PingCode REST API work items, releases, and assignment workflows.",
  "type": "module",
  "bin": {
    "pingcode-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
